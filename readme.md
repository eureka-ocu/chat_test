# chat test

websocketでチャット

## dependencies

### [direnv](https://github.com/direnv/direnv)

```
$ brew install direnv
$ cp _envrc .envrc
$ vim .envrc
// edit .envrc
$ direnv allow
// then some env variables are set
```
